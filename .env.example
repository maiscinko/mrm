# =====================================================
# MRM (Mentoring Relationship Manager) - Environment Variables
# Version: 1.0 (Production-Ready MVP)
# =====================================================

# INSTRUCTIONS:
# 1. Copy this file to .env.local (for local development)
# 2. Copy this file to .env (for production - Vercel)
# 3. Replace all placeholder values with real credentials
# 4. NEVER commit .env.local or .env to git

# =====================================================
# SUPABASE (Database + Auth)
# =====================================================

# Get these from: Supabase Dashboard → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Service role key (NEVER expose to client - use only in API routes)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# =====================================================
# OPENAI (AI Summaries + Provocative Questions)
# =====================================================

# Get from: https://platform.openai.com/api-keys
# Model: GPT-4o (for session summaries and questions)
OPENAI_API_KEY=sk-proj-...

# Optional: Organization ID (if using multiple orgs)
# OPENAI_ORG_ID=org-...

# =====================================================
# ANTHROPIC (AI Renewal Plans)
# =====================================================

# Get from: https://console.anthropic.com/settings/keys
# Model: Claude 3.5 Sonnet (for renewal plan generation)
ANTHROPIC_API_KEY=sk-ant-api03-...

# =====================================================
# RESEND (Email Notifications)
# =====================================================

# Get from: https://resend.com/api-keys
# Used for: renewal alerts, session reminders
RESEND_API_KEY=re_...

# Email sender (must be verified domain in Resend)
RESEND_FROM_EMAIL=noreply@mrm.app
RESEND_FROM_NAME=MRM - Mentoring Relationship Manager

# =====================================================
# APPLICATION SETTINGS
# =====================================================

# Public app URL (used for OAuth redirects, email links)
# Local: http://localhost:3000
# Production: https://mrm.app (or your domain)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# App name (used in emails, metadata)
NEXT_PUBLIC_APP_NAME=MRM

# =====================================================
# OAUTH PROVIDERS (Configured in Supabase Auth)
# =====================================================

# Google OAuth (Get from: https://console.cloud.google.com/apis/credentials)
# No env vars needed - configured in Supabase Dashboard → Authentication → Providers
# Just add OAuth Client ID + Secret in Supabase UI

# LinkedIn OAuth (Get from: https://www.linkedin.com/developers/apps)
# No env vars needed - configured in Supabase Dashboard → Authentication → Providers
# Just add Client ID + Secret in Supabase UI

# =====================================================
# NEXT.js SETTINGS
# =====================================================

# Node environment
NODE_ENV=development

# Next.js telemetry (disable for privacy)
NEXT_TELEMETRY_DISABLED=1

# =====================================================
# FEATURE FLAGS (Optional - for gradual rollout)
# =====================================================

# Enable/disable AI features (if API keys not ready yet)
FEATURE_AI_SUMMARIES=true
FEATURE_AI_QUESTIONS=true
FEATURE_AI_RENEWAL_PLANS=true

# Enable/disable email notifications
FEATURE_EMAIL_NOTIFICATIONS=true

# Enable/disable MLS branding
FEATURE_MLS_PARTNERSHIP=true

# =====================================================
# SECURITY (Optional - for production)
# =====================================================

# JWT secret (if using custom auth - Supabase handles this)
# JWT_SECRET=your-super-secret-jwt-key

# Rate limiting (requests per minute per IP)
RATE_LIMIT_RPM=60

# =====================================================
# ANALYTICS (Optional - for tracking)
# =====================================================

# Google Analytics 4 (if using)
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# PostHog (if using)
# NEXT_PUBLIC_POSTHOG_KEY=phc_...
# NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com

# =====================================================
# MONITORING (Optional - for production)
# =====================================================

# Sentry (error tracking)
# NEXT_PUBLIC_SENTRY_DSN=https://...@sentry.io/...
# SENTRY_AUTH_TOKEN=...

# Vercel Analytics (automatically enabled if deployed to Vercel)
# No env vars needed

# =====================================================
# DATABASE (Additional - if needed)
# =====================================================

# Direct Postgres connection (rarely needed - Supabase handles this)
# Only use if bypassing Supabase client (not recommended)
# DATABASE_URL=postgresql://postgres:[PASSWORD]@db.[PROJECT].supabase.co:5432/postgres

# =====================================================
# DEVELOPMENT ONLY
# =====================================================

# Log level (debug, info, warn, error)
LOG_LEVEL=debug

# Pretty print logs (true in dev, false in prod)
LOG_PRETTY=true

# Mock AI responses (for development without API keys)
MOCK_AI_RESPONSES=false

# =====================================================
# NOTES
# =====================================================

# REQUIRED for MVP:
# - NEXT_PUBLIC_SUPABASE_URL
# - NEXT_PUBLIC_SUPABASE_ANON_KEY
# - SUPABASE_SERVICE_ROLE_KEY
# - OPENAI_API_KEY
# - ANTHROPIC_API_KEY
# - NEXT_PUBLIC_APP_URL

# OPTIONAL for MVP:
# - RESEND_API_KEY (can launch without emails, add later)
# - Feature flags (all default to true)
# - Analytics/Monitoring (add after PMF)

# NEVER COMMIT:
# - .env.local (local development)
# - .env (production secrets)
# Only commit .env.example (this file) with placeholder values

# =====================================================
# END OF CONFIGURATION
# =====================================================
